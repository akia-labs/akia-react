# Required in this environment because the default pnpm store path under home is read-only.
store-dir=/tmp/pnpm-store
# Required in this environment because packageManager resolution tries to fetch pnpm metadata
# from the configured registry before install, which is currently unreachable here.
manage-package-manager-versions=false
